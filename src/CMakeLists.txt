if(${CMAKE_VERSION} VERSION_GREATER_EQUAL "3.11.0")
    file(GLOB_RECURSE SOURCE_CODE CONFIGURE_DEPENDS *.h *.c)
else()
    file(GLOB SOURCE_CODE *.c *.h)
endif()

set(SOURCE_CODE "${SOURCE_CODE}" PARENT_SCOPE)
