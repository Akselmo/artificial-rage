Added support for player speed and camera sensitivity

raylib.h
    RLAPI void UpdateCamera(Camera *camera, float speed, float sensitivity);

camera.h
    //Comment related defines out

    void UpdateCamera(Camera *camera, float speed, float sensitivity)
    {
        //Added these for speed and sensitivity
        float PLAYER_MOVEMENT_SENSITIVITY = speed;
        float CAMERA_MOUSE_MOVE_SENSITIVITY = sensitivity;
        ...
    }

---





